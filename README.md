# 🎫 Savory Bistro - Premium Online Table Reservation System

> ระบบจัดการจองโต๊ะแบบครบวงจร พร้อมหน้าจอ Admin และ Customer ที่ใช้งานง่าย

[![Next.js](https://img.shields.io/badge/Next.js-16-black)](https://nextjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue)](https://www.typescriptlang.org/)
[![Supabase](https://img.shields.io/badge/Supabase-2.0-green)](https://supabase.com/)

---

## 🚀 อัปเดตล่าสุด (v1.3) - *Current Version*

ยกระดับความปลอดภัยและระบบตรวจสอบ (Deep Audit):
- **Deep Audit Logging:** บันทึกทุกการกระทำของ Admin (พนักงาน, โต๊ะ, การจอง, วันหยุด, รหัสผ่าน) ลงในระบบ Audit Trail เพื่อความโปร่งใสสูงสุด
- **Centralized Holidays API:** ระบบจัดการวันหยุดร้านแบบ Server-side API พร้อมระบบตรวจสอบประวัติการแก้ไข
- **Dynamic Settings:** ปรับค่าระยะเวลาทานอาหาร (Dining Duration) และ Buffer Time ได้ทันทีผ่านหน้า Admin โดยไม่ต้องแก้ไขโค้ด
- **Secure API Layer:** ปรับปรุงความปลอดภัยโดยเปลี่ยนการเขียนข้อมูลตรงจาก Frontend เป็นการเรียกผ่าน Secure API ทั้งหมด

---

## 🕒 อัปเดตก่อนหน้า

### v1.2 - Stability & Privacy
- **Instant Feedback:** ระบบรีวิวจากลูกค้าแสดงผล Real-time ผ่าน Supabase Realtime
- **Privacy First:** ระบบ Masking ข้อมูลส่วนตัวลูกค้าใน API สาธารณะ
- **Robust Rate Limiting:** ปรับปรุงระบบจำกัดการเรียก API (Upstash) ให้เสถียรยิ่งขึ้น

### v1.1 - Experience & AI
- **AI Genius:** อัปเกรดเป็น **Gemini 2.5 Flash** ประมวลผลรวดเร็วและแม่นยำ
- **Mobile-First Admin:** ตัวจัดการร้านรองรับการใช้งานผ่านมือถือสมบูรณ์แบบ
- **Staff Note:** ระบบบันทึกข้อความภายในสำหรับพนักงาน

## ✨ ฟีเจอร์หลัก

### 👤 สำหรับลูกค้า

- **จองโต๊ะง่ายๆ** - เลือกวัน เวลา และโต๊ะจากผังร้านแบบ Interactive ที่เข้าใจง่าย
- **2 ภาษา** - รองรับทั้งภาษาไทยและอังกฤษ ตอบโจทย์ลูกค้าทุกกลุ่ม
- **ยืนยันทันที** - ระบบชำระเงินมัดจำผ่าน QR PromptPay พร้อมอัปโหลดสลิป
- **AI Assistant** - ให้ AI ช่วยเลือกโต๊ะที่ "ใช่" ที่สุดสำหรับคุณ (Powered by Gemini 2.5)
- **ติดตามสถานะ** - เช็คผลการจองได้ตลอดเวลาผ่านรหัส BX-XXXXXX

### 🔐 สำหรับพนักงาน/Admin

- **Enhanced Dashboard** - ดูภาพรวมและกราฟแนวโน้มลูกค้า (Peak Occupancy) แบบ Real-time
- **Reservation Management** - จัดการรายการจองได้รวดเร็ว พร้อมฟิลเตอร์กรองสถานะ/วันที่ และช่อง **Staff Note** สำหรับบันทึกเพิ่มเติม
- **Professional Slips** - พิมพ์ใบยืนยันการจองรูปแบบใหม่ (Thermal Printer Friendly) ดูเป็นมืออาชีพ
- **Table Layout 2.0** - ปรับผังร้านได้ยืดหยุ่น ลาก-วางโต๊ะได้ดั่งใจ
- **Security Check** - ระบบตรวจสอบสิทธิ์ Admin/Staff แยกชัดเจน ปลอดภัยหายห่วง

---

## 🚀 การติดตั้ง

### ข้อกำหนดระบบ

- Node.js 20+ (แนะนำ 20 LTS หรือล่าสุด)
- Supabase Account
- Upstash Redis Account
- LINE Notify Token (Optional)

### ขั้นตอนการติดตั้ง

1. **Clone โปรเจค**
   ```bash
   git clone <repository-url>
   cd table-reserve
   ```

2. **ติดตั้ง Dependencies**
   ```bash
   npm install
   ```

3. **ตั้งค่า Environment Variables**
   
   สร้างไฟล์ `.env.local` โดยคัดลอกตัวอย่างจาก `.env.example` หรือกรอกข้อมูลดังนี้:
   ```env
   # --- 1. Supabase (Database & Auth) ---
   NEXT_PUBLIC_SUPABASE_URL=...
   NEXT_PUBLIC_SUPABASE_ANON_KEY=...
   SUPABASE_SERVICE_ROLE_KEY=...  # Required for Admin API

   # --- 2. Upstash Redis (Rate Limiting) ---
   UPSTASH_REDIS_REST_URL=...
   UPSTASH_REDIS_REST_TOKEN=...

   # --- 3. Google Gemini AI (Table Recommendation) ---
   GEMINI_API_KEY=...

   # --- 4. Notifications (Optional) ---
   LINE_CHANNEL_ACCESS_TOKEN=...  # LINE Messaging API
   LINE_TARGET_ID=...             # Admin User/Group ID for alerts
   RESEND_API_KEY=...             # Email Service

   # --- 5. Database Connection ---
   # Connect to Supabase via Transaction Pooler
   DATABASE_URL="postgres://postgres.[YOUR-PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

   # Connect to Supabase directly (Session Mode)
   DIRECT_URL="postgres://postgres.[YOUR-PROJECT-REF]:[PASSWORD]@aws-0-[REGION].supabase.com:5432/postgres"

4. **Setup Database**
   - รันคำสั่ง `npx prisma db push` เพื่อสร้างตารางและซิงค์ข้อมูล
   
5. **รันโปรเจค**

   **แบบ Production (Docker):**
   ```bash
   docker compose up -d
   ```
   
   **แบบ Development:**
   ```bash
   npm run dev
   ```

---

## 📦 Deploy

แนะนำให้ใช้ **Vercel** เพื่อความง่ายและรวดเร็วที่สุด หรือ **Docker** สำหรับ Private Server (รายละเอียดเพิ่มเติมดูในส่วน Deployment Guide)

---

## 🛠️ เทคโนโลยีที่ใช้

| Technology | Purpose |
|------------|---------|
| **Next.js 16** | Frontend Framework (App Router) ที่ทันสมัยที่สุด |
| **Google Gemini 2.5** | AI Model รุ่นล่าสุดสำหรับการประมวลผลอัจฉริยะ |
| **Supabase** | ฐานข้อมูล PostgreSQL และระบบ Authentication |
| **Tailwind CSS v4** | จัดการดีไซน์และ Responsive แบบ CSS-First |
| **Upstash Redis** | ระบบ Rate Limiting และจัดการ Slot Hold |
| **Prisma** | ORM สำหรับจัดการ Schema ฐานข้อมูลอย่างเป็นระบบ |
| **Docker** | Containerization สำหรับการติดตั้งที่ง่ายและเสถียร |

---

## 📚 โครงสร้างโปรเจค

```
table-reserve/
├── src/
│   ├── app/
│   │   ├── admin/          # Admin Panel (Dashboard, Settings, etc.)
│   │   ├── api/            # API Routes
│   │   ├── reservation-form/  # Customer Booking Flow
│   │   └── page.tsx        # Homepage
│   ├── components/         # Reusable Components
│   ├── lib/               # Utilities (Supabase, i18n, notifications)
│   └── types/             # TypeScript Types
└── public/                # Static Assets
```

---
---

## 🔌 API Documentation

รายการ API ที่สามารถเรียกใช้งานได้ผ่านเว็บไซต์ (Frontend) และ API สำหรับการจัดการหลังบ้าน (Admin).

### 🌐 Public APIs (Frontend Callable)

API เหล่านี้เปิดให้หน้าเว็บไซต์เรียกใช้งานได้โดยไม่ต้อง Login (บางตัวมีการจำกัด Rate Limit):

| Method | Endpoint | Description |
| :--- | :--- | :--- |
| `POST` | `/api/reservations` | **สร้างการจองใหม่** รองรับข้อมูลลูกค้า, จำนวนแขก, และสลิปโอนเงิน |
| `POST` | `/api/ai/recommend-table` | **ขอคำแนะนำโต๊ะจาก AI** โดยส่งจำนวนแขกและความต้องการพิเศษ |
| `GET` | `/api/tables` | **ดึงข้อมูลโต๊ะทั้งหมด** แสดงรายการโต๊ะและสถานะความว่าง |
| `GET` | `/api/timeslots` | **ดึงช่วงเวลาที่จองได้** เช็ค Time Slots ที่ว่างในแต่ละวัน |
| `GET` | `/api/public/check-booking` | **ตรวจสอบสถานะการจอง** ใช้ Booking Code (เช่น BX-123456) |
| `GET` | `/api/public/feedback` | **ดึงรีวิวลูกค้า** แสดง Testimonials หน้าเว็บ (เฉพาะ 4-5 ดาว) |
| `POST` | `/api/feedback` | **ส่งรีวิวความพึงพอใจ** หลังจากลูกค้าทานอาหารเสร็จ |

### 🛡️ Protected APIs (Admin/Internal Only)

API เหล่านี้ต้องใช้ **Service Role Key** หรือ **Admin Session** ในการเข้าถึง:

| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/admin/dashboard` | ข้อมูลสรุปยอดขายและจำนวนแขก | ✅ Admin |
| `GET/POST`| `/api/settings` | จัดการการตั้งค่าร้าน และ Dynamic Logic | ✅ Admin |
| `GET/POST/DELETE`| `/api/holidays` | **จัดการวันหยุดร้าน** (แทนที่การเขียน DB ตรง) | ✅ Admin |
| `GET/POST/PUT/DELETE`| `/api/staff` | จัดการพนักงานและสิทธิ์การเข้าถึง | ✅ Admin |
| `POST` | `/api/change-password` | เปลี่ยนรหัสผ่าน Admin/Staff ปลอดภัย | ✅ Auth |
| `GET` | `/api/cron/auto-cancel` | ยกเลิกการจองที่ค้างนานเกิน 30 นาที | 🔑 Cron Secret |
| `POST` | `/api/ai/daily-insights` | วิเคราะห์ข้อมูลรายวันด้วย AI | 🔑 Cron Secret |
| `DELETE` | `/api/feedback` | ลบรีวิวที่ไม่เหมาะสม | ✅ Admin |

---


## 🤝 สนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
- 📧 Email: tanakorn488@outlook.com
- 🐛 Issues: [GitHub Issues](https://github.com/tanakorn911/table-reserve/issues)

---

---

## 👥 คณะผู้จัดทำ

| รหัสนิสิต | ชื่อ-นามสกุล (TH) | Name (EN) |
|------------|-------------------|-----------|
| **67020982** | นายธนกร แก้วใหม่ | Tanakorn Kaewmai |
| **67024717** | นายกิตติพัฒน์ ยศวงค์ | Kittiphat Yotwong |
| **67024740** | นายชนสรณ์ เผ่าชวด | Chanasorn Phaochoad |

---

## 📄 License

MIT License - ใช้งานได้อย่างอิสระ

---

**สร้างด้วย ❤️ สำหรับประสบการณ์การจองโต๊ะที่ยอดเยี่ยม**
