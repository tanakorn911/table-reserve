# 🎫 จองโต๊ะออนไลน์ (BookingX)

ระบบจัดการจองโต๊ะร้านอาหารแบบครบวงจร พัฒนาด้วย Next.js 15, TypeScript, Tailwind CSS และ Supabase

## 🚀 ฟีเจอร์ที่เพิ่มเข้ามาล่าสุด (Latest Updates)

- **ระบบ 2 ภาษา (i18n):** รองรับการใช้งานทั้งภาษาไทยและภาษาอังกฤษ (TH/EN) แบบสมบูรณ์
- **ระบบตรวจสอบเวลาทำการ:** ดึงข้อมูลเวลาจากฐานข้อมูลแบบ Real-time พร้อมระบบสำรอง (Fallback)
- **ระบบแผนที่ Interactive:** ปรับปรุงพิกัดร้านให้แม่นยำ พร้อมการแสดงผลแบบ High-fidelity
- **ความเสถียรของข้อมูล:** ระบบป้องกันการจองซ้ำซ้อนด้วยเทคนิค Data Normalization และ Time-buffer ตรวจสอบสถานะโต๊ะทุก 3 วินาที
- **ระบบ Login ที่ราบรื่น:** หน้า Loading State แบบ Full-screen และการจัดการ Session ที่ปลอดภัยยิ่งขึ้น

## 🚀 ฟีเจอร์หลัก (Features)

### สำหรับลูกค้า (Customer Side)
- **Landing Page:** หน้าแรกที่สวยงามทันสมัย พร้อมข้อมูลร้านและโปรโมชั่นที่ดึงข้อมูลสดจากระบบ
- **จองโต๊ะออนไลน์:** เลือกวันที่ เวลา และเลือกโต๊ะจากแผนผังร้านจริง (Interactive Floor Plan)
- **ชำระเงินมัดจำ:** ระบบสแกน QR Code PromptPay และอัปโหลดสลิปเพื่อยืนยันการจอง
- **ตรวจสอบสถานะ:** เช็คสถานะการจอง (Pending/Confirmed) ได้ทันทีผ่านรหัสจอง `BX-XXXXXX`
- **Multi-Language:** สลับภาษาได้ทันทีโดยไม่ต้องรีโหลดหน้าเว็บ

### สำหรับพนักงานและผู้ดูแลระบบ (Staff/Admin Side)
- **Dashboard:** ดูภาพรวมการจองรายวัน และPeak Hours แบบกราฟ
- **ระบบจัดการการจอง:** ตรวจสอบสลิปเงินโอน, อนุมัติ/ยกเลิกการจอง และพิมพ์ใบบันทึกการจอง
- **จัดการข้อมูลโต๊ะ:** แก้ไขสถานะโต๊ะและตำแหน่งโต๊ะได้แบบ Real-time
- **ตั้งค่าระบบ:** จัดการเวลาเปิด-ปิดร้าน และประกาศวันหยุดพิเศษ (Holiday Announcements)
- **RBAC Security:** แบ่งสิทธิ์การใช้งานชัดเจน (Admin จัดการตั้งค่าได้ / Staff จัดการหน้างานได้)

## 🛠️ การติดตั้งและใช้งาน (Installation)

1. **ติดตั้ง Dependencies:**
   ```bash
   npm install
   ```

2. **ตั้งค่า Environment Variables:**
   สร้างไฟล์ `.env.local` และเพิ่มค่าจาก Supabase และ LINE API:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
   LINE_CHANNEL_ACCESS_TOKEN=your_token
   LINE_TARGET_ID=your_id
   ```

3. **รันระบบ Development:**
   ```bash
   npm run dev
   ```
   เปิดระบบที่ [http://localhost:4028](http://localhost:4028)

## 📁 โครงสร้างโปรเจกต์ (Project Structure)

```
bookingx/
├── src/
│   ├── app/                # Next.js App Router (Admin, Customer, API)
│   ├── components/         # UI Components & Common Elements
│   ├── lib/                # i18n, Global Utils & Database Logic
│   ├── contexts/           # Global States (Locale, Navigation)
│   ├── styles/             # Tailwind & Global CSS
│   └── types/              # TypeScript Interfaces
├── supabase/               # SQL Scripts สำหรับตั้งค่าฐานข้อมูล & DB Schema
└── public/                 # Static Assets & Global Icons
```

## 🚀 การ Deploy ขึ้น Vercel (Deployment)

คุณสามารถนำโปรเจกต์นี้ขึ้นออนไลน์ได้ง่ายๆ ผ่าน **Vercel**:

1. **Push โค้ดลงบน GitHub:** สร้าง Repository และอัปโหลดไฟล์ทั้งหมดขึ้นไป
2. **Import Project ใน Vercel:** เลือก Repository ดังกล่าว
3. **ตั้งค่า Environment Variables:** นำค่าจากไฟล์ `.env` ไปใส่ในหน้า Settings ของ Vercel (จำเป็นอย่างยิ่งเพื่อให้ระบบฐานข้อมูลและแจ้งเตือนทำงานได้)
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `LINE_CHANNEL_ACCESS_TOKEN`
   - `LINE_TARGET_ID`
4. **Build & Deploy:** กดปุ่ม Deploy และรอระบบสร้าง URL ให้ใช้งาน

## 🔒 ข้อมูลทางเทคนิค (Technical Spec)
- **Framework:** Next.js 15 (App Router)
- **Database:** Supabase (PostgreSQL + Real-time)
- **Auth:** Supabase Auth (JWT + RBAC)
- **Styling:** Tailwind CSS (Custom Design System)
- **i18n:** Custom Memoized Translation Hook
- **State Management:** React Context API + URL State

## ขอบคุณ
- ทีมพัฒนา Deepseek & Google Deepmind สำหรับเครื่องมือช่วยเขียนโปรแกรม
- Next.js & Tailwind CSS ทีมงานที่สร้างเฟรมเวิร์คที่ยอดเยี่ยม
- มหาวิทยาลัยพะเยา (University of Phayao) สำหรับสถานที่และข้อมูล

---
สร้างด้วย ❤️ เพื่อประสบการณ์การจองโต๊ะที่ยอดเยี่ยม